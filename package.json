{
  "name": "socket",
  "version": "1.0.0",
  "description": "socketIO using raw nodejs server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vaibhav",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^1.7.3"
  }
}
